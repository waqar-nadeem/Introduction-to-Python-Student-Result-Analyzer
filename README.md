# Introduction-to-Python-Student-Result-Analyzer
This Python program calculates a student’s average marks across eight subjects. It uses a for loop to collect input iteratively, ensuring scalability if subject counts change. Type casting converts user input to integers. Conditional control structures can be added for validation and data checks.
